using Verse;

namespace HumanResources
{
    public class CompProperties_ShootingArea : CompProperties
    {
        public CompProperties_ShootingArea()
        {
            compClass = typeof(CompShootingArea);
        }
    }
}
